# Host and port for the Actix server
APP_HOST=127.0.0.1
APP_PORT=8000

# Logging level for the application (e.g., info, debug, warn, error)
# You can also set module-specific levels: RUST_LOG="info,src_backend=debug,actix_web=info"
RUST_LOG="info"

# URL for the Autonomi network node the backend will connect to.
# For local development, this might point to a locally running Autonomi node.
# For production, this must be a stable, accessible node (testnet or mainnet).
AUTONOMI_NETWORK_URL="http://localhost:8645"

# A secret key for the application. Used for signing sessions, JWTs, or other cryptographic needs.
# Generate a strong, random key for production. For local dev, a placeholder is fine.
# Example (generate with `openssl rand -hex 32` or similar):
SECRET_KEY="your_strong_random_secret_key_here_for_dev_or_generate_one"

# Add any other environment variables your autonomi-core or handlers might need. 